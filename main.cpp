#include "map.hpp"
#include <iostream>
#include <map>

using namespace std;

int main()
{ 
    ft::map<string, string> map;
    // std::map<string,string> first;

    // first.insert(std::pair<string,string>("name","hamid"));

    // cout << first["name"];

    return 0;
}